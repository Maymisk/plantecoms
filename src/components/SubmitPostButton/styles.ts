import styled from 'styled-components';

export const Button = styled.button`
    width: 100%;
    color: black;

    font-size: 1.25rem;
    font-weight: bold;

    @media (max-width: 400px) {
        font-size: 0.9rem;
    }
`;
